# Core dependencies
pyyaml>=6.0

# Optional dependencies for specific adapters
openpyxl>=3.0.0        # Excel (.xlsx, .xls) support
pyarrow>=12.0.0       # Parquet, ORC, Feather support
fastavro>=1.8.0      # Avro support
h5py>=3.8.0          # HDF5 support
beautifulsoup4>=4.12.0  # HTML table extraction
lxml>=4.9.0          # XML parsing (faster than built-in)

# Optional dependencies for channels
requests>=2.31.0     # API channel HTTP requests
kafka-python>=2.0.2  # Kafka stream support
boto3>=1.28.0        # AWS Kinesis, AWS IoT support
paho-mqtt>=1.6.1     # MQTT support
pika>=1.3.0          # RabbitMQ support
websocket-client>=1.6.0  # WebSocket support

# Optional dependencies for database channels
psycopg2-binary>=2.9.0  # PostgreSQL support
pymysql>=1.1.0         # MySQL support
pyodbc>=4.0.0          # SQL Server support

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
